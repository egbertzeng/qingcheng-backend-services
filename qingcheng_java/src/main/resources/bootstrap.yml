#二、服务自身配置
spring:
  application:
    name: qingcheng_java  #对应所获取的配置文件的{application}
#  cloud:
#    config:
#      profile: ${CURRENT_ACTIVE_PROFILE:dev}
#      label: master
#      discovery:
#        enabled: true                     # 默认false，设为true表示使用注册中心中的configserver配置而不自己配置configserver的uri
#        serviceId: configuration-service  # 指定config server在服务发现中的serviceId，默认为：configserver
#三、基础服务配置
#1.eureka信息
eureka:
  instance:
    preferIpAddress: true
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER_HOST_01:localhost}:${EUREKA_SERVER_PORT:8000}/eureka/